# MCP Core
mcp>=1.0.0

# Database drivers
psycopg[binary]>=3.1.0
asyncpg>=0.31.0
supabase>=2.31.0
sqlalchemy>=2.0.49
greenlet>=3.5.0

# HTTP and API
aiohttp>=3.9.0
requests>=2.32.0  # shared/grid_design exchange_rate lookup
fastapi>=0.139.0
uvicorn>=0.24.0

# MQTT (for VRM equipment control)
paho-mqtt>=2.0.0

# Authentication and Security
PyJWT>=2.12.1
google-auth>=2.0,<3  # Required by shared/ for Google Docs/Drive authentication
google-api-python-client>=2.198.0,<3  # Required by shared/ for Google Docs API
google-genai>=1.0,<3.0  # Gemini (Developer API) + embeddings (Vertex AI backend)

# Data processing
pydantic>=2.4.0

# Utilities
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
loguru>=0.7.3
rapidfuzz>=3.14.5  # Fuzzy string matching for grid names
pdfplumber>=0.11.9  # PDF table extraction for import standards document

# Scheduling
pytz>=2024.1
croniter>=6.2.2

# Visualization
vl-convert-python>=1.9.0.post1

# Image processing
Pillow>=10.0.0
tavily-python

# Geospatial (geo hazard lookups — WRI Aqueduct Floods + Copernicus DEM via COG range requests)
rasterio>=1.4.4
s2sphere>=0.2.5
