"""NiceGUI admin app — successor to the retired Streamlit `app.py`.

App shell, Google OAuth (Authlib), RBAC-gated navigation, `/healthz`, and every
page (chat, documents, agents, settings, broadcast, grid design).
"""
