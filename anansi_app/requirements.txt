# NiceGUI is the app (see nicegui_app/, start.sh).
nicegui>=2.0.0
authlib>=1.3.0
itsdangerous>=2.2.0  # session cookie signing used by nicegui's app.storage.user
supabase>=2.0.0
python-dotenv>=1.0.0
pandas>=3.0.2
pytz>=2026.2
requests>=2.33.1
markdown>=3.10.2
types-Markdown>=3.10.2.20260408
asyncpg>=0.31.0
httpx>=0.28.1
croniter>=6.2.3  # recurring broadcasts need '#' (nth weekday) and 'L' cron syntax
loguru>=0.7.3
google-api-python-client>=2.194.0,<3
google-auth>=2.55.2,<3

# Required by shared.config.settings (pydantic BaseSettings), pulled in via
# settings_service.py -> shared.config. Mirrors mcp_servers/requirements.txt.
pydantic>=2.4.0
pydantic-settings>=2.14.2
