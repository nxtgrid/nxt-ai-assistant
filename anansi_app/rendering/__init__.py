"""Framework-agnostic rendering helpers shared by the Streamlit and NiceGUI UIs.

These modules contain pure functions (no ``streamlit``/``nicegui`` imports) so the
same conversation/message HTML can be produced from either front end during the
Streamlit -> NiceGUI migration.
"""
